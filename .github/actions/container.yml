name: 'Container'
description: 'Runs the Dockerfile for Sigma Linux'
inputs:
  exec:
    description: 'Shell command to run'
    required: true
    default: './build.sh && cp /app/build/sigma-linux.iso /github/workspace'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ exec }}
