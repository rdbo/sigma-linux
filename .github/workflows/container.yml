name: 'Container'
description: 'Runs the Dockerfile for Sigma Linux'
inputs:
  run:
    description: 'Shell commands to run'
    required: true
    default: './build.sh'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ run }}
